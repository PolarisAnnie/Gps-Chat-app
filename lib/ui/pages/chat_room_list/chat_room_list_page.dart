import 'package:flutter/material.dart';

class ChatRoomListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('chat_room_list_page')));
  }
}